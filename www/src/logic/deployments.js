import { graphql } from "react-apollo";
import gql from "graphql-tag";

// const registerMutation = gql`
//   mutation register($user: UserRegister!) {
//     register(user: $user) {
//       userName
//       password
//     }
//   }
// `;
// const registerMutationOptions = {
//   name: "register",
// };
// export const register = graphql(registerMutation, registerMutationOptions);
